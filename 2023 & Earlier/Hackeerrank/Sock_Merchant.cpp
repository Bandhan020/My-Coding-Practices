#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n,i,j,arr[100],arr1[100];
    cin>>n;
    for(i=0;i<n;i++){
        cin>>arr[i];
    }
    memset(arr1,0,sizeof(arr1));

    return 0;
}
