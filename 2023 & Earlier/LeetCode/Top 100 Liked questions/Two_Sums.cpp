#include <bits/stdc++.h>
using namespace std;


vector<int> twoSum(vector<int>& nums, int target) {
        vector<int>sum;

    }

int main()
{
    vector<int>num,sum1;
    int n,i,j,target;
    cin>>n;
    for(i=0;i<n;i++){
        cin>>num[i];
    }
    cin>>target;
    sum1= twoSum(num,target);
    return 0;
}